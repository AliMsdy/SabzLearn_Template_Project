//components
import { TopBar } from "@/Components/TopBar";
import { Navbar } from "@/Components/Navbar";

function Header() {
  return (
    <header>
      <TopBar />
      <Navbar />
    </header>
  );
}

export { Header };
